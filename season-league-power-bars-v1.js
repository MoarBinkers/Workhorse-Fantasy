(()=>{
'use strict';
if(window.__WH_LEAGUE_POWER_BARS_V1__)return;window.__WH_LEAGUE_POWER_BARS_V1__=true;
const SLEEPER='https://api.sleeper.app/v1',SB='https://ytfwbvdzhrebupcftmhs.supabase.co',KEY='sb_publishable_5BYaizAtZ_XkjXaVSFPk0w_v2qap-8k',ORDER_KEY='wh_ros_master_v3',CTX='wh_sleeper_context_v1',POS=['QB','RB','WR','TE'];
const COLORS={QB:'#a98bff',RB:'#59d694',WR:'#5da8ff',TE:'#f0a45d'};
const OFFENSE_SLOTS=new Set(['QB','RB','WR','TE','FLEX','SUPER_FLEX','WRRB_FLEX','REC_FLEX','WRRBTE_FLEX']);
const state={leagueId:null,league:null,rosters:[],pool:new Map(),rankMap:new Map(),posRankMap:new Map(),teams:new Map(),loading:false,scale:{overall:1,pos:{QB:1,RB:1,WR:1,TE:1}}};
const load=(k,f)=>{try{const v=JSON.parse(localStorage.getItem(k)||'null');return v??f}catch(_){return f}};
const order=()=>{const a=load(ORDER_KEY,[]);return Array.isArray(a)?a.map(String):[]};
function styles(){if(document.querySelector('#wh-power-bars-css'))return;document.head.insertAdjacentHTML('beforeend',`<style id="wh-power-bars-css">
.power-bar{height:17px!important;border-radius:999px!important;background:#0d1b24!important;box-shadow:inset 0 0 0 1px #29414f!important;overflow:hidden!important}.power-fill.wh-stacked-power{display:flex!important;height:100%!important;width:var(--wh-abs-width)!important;background:transparent!important;box-shadow:none!important;border-radius:999px!important;overflow:hidden!important;transform-origin:left center;animation:whAbsoluteGrow .72s cubic-bezier(.16,1,.3,1) forwards!important}.team .power-fill.wh-stacked-power{background:transparent!important}.wh-power-seg{height:100%;min-width:0;position:relative;box-shadow:inset -1px 0 #07101866;transition:filter .18s,flex-basis .45s cubic-bezier(.16,1,.3,1)}.wh-power-seg:first-child{border-radius:999px 0 0 999px}.wh-power-seg:last-child{border-radius:0 999px 999px 0}.wh-power-seg:only-child{border-radius:999px}.wh-power-seg:hover{filter:brightness(1.15)}.wh-power-seg.qb{background:${COLORS.QB}}.wh-power-seg.rb{background:${COLORS.RB}}.wh-power-seg.wr{background:${COLORS.WR}}.wh-power-seg.te{background:${COLORS.TE}}@keyframes whAbsoluteGrow{from{transform:scaleX(0)}to{transform:scaleX(1)}}.wh-pos-legend{display:inline-flex;gap:10px;align-items:center;flex-wrap:wrap;margin-left:8px}.wh-pos-key{display:inline-flex;align-items:center;gap:4px;font-size:6.7px;font-weight:900;color:#8498a3}.wh-pos-dot{width:7px;height:7px;border-radius:2px}.wh-pos-dot.qb{background:${COLORS.QB}}.wh-pos-dot.rb{background:${COLORS.RB}}.wh-pos-dot.wr{background:${COLORS.WR}}.wh-pos-dot.te{background:${COLORS.TE}}.bar-meta span:last-child{display:none!important}.power-num{display:none!important}.team .bar-wrap{position:relative}@media(min-width:851px){.team-row{grid-template-columns:42px 46px minmax(145px,.52fr) minmax(430px,3fr)!important}}@media(max-width:560px){.power-bar{height:15px!important}.wh-pos-legend{display:flex;margin:5px 0 0;gap:8px}}
</style>`)}
async function getJson(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`Power bars ${r.status}`);return r.json()}
async function loadPool(){if(state.pool.size)return;const r=await fetch(`${SB}/rest/v1/sleeper_adp_current?select=player_id,full_name,position,team,sleeper_rank,position_rank&format=eq.ppr&limit=700`,{headers:{apikey:KEY,Accept:'application/json'},cache:'no-store'});if(!r.ok)throw new Error(`Power bar pool ${r.status}`);for(const p of await r.json())if(p?.player_id)state.pool.set(String(p.player_id),p);const counts={};order().forEach((id,i)=>{const key=String(id),p=state.pool.get(key);state.rankMap.set(key,i+1);if(p?.position){counts[p.position]=(counts[p.position]||0)+1;state.posRankMap.set(key,counts[p.position])}})}
function rankFor(id,p){return state.rankMap.get(String(id))||Number(p?.sleeper_rank)||999}
function posRankFor(id,p){return state.posRankMap.get(String(id))||Number(p?.position_rank)||999}
function scoreForRank(r){const x=Math.max(0,r-1);return 5+210*Math.exp(-.022*x)+100*Math.exp(-.085*x)}
function rosterSlots(){return Array.isArray(state.league?.roster_positions)?state.league.roster_positions:[]}
function leagueSize(){return Math.max(2,Number(state.league?.total_rosters)||state.rosters.length||12)}
function starterSlots(pos){const direct=rosterSlots().filter(x=>x===pos).length;return direct||({QB:1,RB:2,WR:2,TE:1}[pos]||1)}
function flexCounts(){const s=rosterSlots();return{flex:s.filter(x=>['FLEX','WRRBTE_FLEX','REC_FLEX'].includes(x)).length,wrrb:s.filter(x=>x==='WRRB_FLEX').length,superflex:s.filter(x=>x==='SUPER_FLEX').length}}
function replacementRank(pos){const n=leagueSize(),base=starterSlots(pos)*n,f=flexCounts();let extra=0;if(pos==='QB')extra=f.superflex*n*.78;if(pos==='RB')extra=(f.flex*n*.38)+(f.wrrb*n*.44)+(f.superflex*n*.08);if(pos==='WR')extra=(f.flex*n*.50)+(f.wrrb*n*.56)+(f.superflex*n*.11);if(pos==='TE')extra=(f.flex*n*.12)+(f.superflex*n*.03);return Math.max(1,Math.round(base+extra))}
function expectedPpr(pos,r,repl){const floor={QB:16,RB:8.8,WR:9.5,TE:7.4}[pos]??8,ceiling={QB:24.5,RB:21,WR:21.5,TE:17}[pos]??17,decay={QB:.16,RB:.085,WR:.075,TE:.15}[pos]??.1;if(r<=repl)return floor+(ceiling-floor)*Math.exp(-decay*Math.max(0,r-1));return Math.max(3,floor-.28*(r-repl))}
function playerValue(pos,overallRank,posRank){const base=scoreForRank(overallRank),repl=replacementRank(pos),edge=expectedPpr(pos,posRank,repl)-expectedPpr(pos,repl,repl),scale={QB:8.5,RB:11.5,WR:10.5,TE:12.5}[pos]||10,vor=Math.max(-28,edge*scale);return Math.max(3,base*.78+vor)}
function backupEliteCut(pos){const n=leagueSize();if(pos==='QB')return Math.max(4,Math.round(n*.42));if(pos==='TE')return Math.max(3,Math.round(n*.33));return n}
function flexDepthBoost(pos,index,posRank){const f=flexCounts(),slots=starterSlots(pos);if(index<slots)return 1;const depth=index-slots;if(pos==='QB'){if(f.superflex&&depth===0)return .82;if(depth>0)return depth===1?.003:.001;const elite=backupEliteCut('QB');if(posRank<=elite)return .05;if(posRank<=leagueSize())return .02;return .005}if(pos==='RB'||pos==='WR'){const hasFlex=(f.flex+f.wrrb+f.superflex)>0;if(hasFlex&&depth===0)return .76;return[.52,.31,.18,.10,.06][hasFlex?depth-1:depth]??.04}if(pos==='TE'){if(depth>0)return depth===1?.02:.008;const elite=backupEliteCut('TE'),upper=Math.max(elite+1,Math.round(leagueSize()*.67));if(posRank<=elite)return .22;if(posRank<=upper)return .08;return .025}return .04}
function usefulDepthSlots(pos){return pos==='RB'||pos==='WR'?3:1}
function offenseStarterCount(){const n=rosterSlots().filter(x=>OFFENSE_SLOTS.has(x)).length;return n||7}
function benchSlotCount(){const n=rosterSlots().filter(x=>x==='BN').length;return n||6}
function metric(r){const all=(r.players||[]).map(id=>{const p=state.pool.get(String(id));if(!p||!POS.includes(p.position))return null;const overallRank=rankFor(id,p),posRank=posRankFor(id,p);return{...p,id:String(id),overallRank,posRank,raw:playerValue(p.position,overallRank,posRank)}}).filter(Boolean),pos={QB:0,RB:0,WR:0,TE:0};let rosterScore=0;for(const position of POS){const group=all.filter(x=>x.position===position).sort((a,b)=>a.posRank-b.posRank||a.overallRank-b.overallRank);group.forEach((p,i)=>{p.depthIndex=i;p.depthWeight=flexDepthBoost(position,i,p.posRank);p.score=p.raw*p.depthWeight;pos[position]+=p.score;rosterScore+=p.score})}return{all,pos,rosterScore}}
function build(){const list=state.rosters.map(r=>({r,m:metric(r),ranks:{},bonus:{},overall:0})),n=Math.max(2,list.length);for(const p of POS)[...list].sort((a,b)=>b.m.pos[p]-a.m.pos[p]).forEach((t,i)=>t.ranks[p]=i+1);for(const t of list){let bonus=0;for(const p of POS){const cap={QB:2,RB:18,WR:18,TE:5}[p];t.bonus[p]=((n-t.ranks[p])/(n-1))*cap;bonus+=t.bonus[p]}t.overall=t.m.rosterScore+bonus}[...list].sort((a,b)=>b.overall-a.overall).forEach((t,i)=>t.ranks.overall=i+1);for(const t of list)state.teams.set(String(t.r.roster_id),t);buildScale()}
function buildScale(){const starters=offenseStarterCount(),usefulBench=Math.min(benchSlotCount(),6);state.scale.overall=Math.max(1,starters*205+usefulBench*34+100);for(const p of POS){const slots=starterSlots(p),depth=usefulDepthSlots(p);state.scale.pos[p]=Math.max(1,slots*220+depth*36+30)}}
function overallPct(t){const ratio=Math.max(0,t.overall/state.scale.overall);return Math.max(30,Math.min(96,100*Math.pow(ratio,.84)))}
function positionPct(t,pos){const ratio=Math.max(0,(t.m.pos[pos]+t.bonus[pos])/state.scale.pos[pos]);return Math.max(24,Math.min(96,100*Math.pow(ratio,.84)))}
function activeView(){return document.querySelector('[data-power-view].active')?.dataset.powerView||'OVERALL'}
function legend(){const key=document.querySelector('.power-key');if(!key)return;if(!key.dataset.whCapacityCopy){key.firstChild&&(key.firstChild.textContent='Power uses the full roster and league lineup limits. Current Sleeper starters/bench do not affect POWER. ');key.dataset.whCapacityCopy='1'}if(key.querySelector('.wh-pos-legend'))return;key.insertAdjacentHTML('beforeend',`<span class="wh-pos-legend">${POS.map(p=>`<span class="wh-pos-key"><i class="wh-pos-dot ${p.toLowerCase()}"></i>${p}</span>`).join('')}</span>`)}
function overallSegments(t){const values=POS.map(p=>({p,v:Math.max(0,t.m.pos[p]+t.bonus[p])})),sum=values.reduce((n,x)=>n+x.v,0)||1;return values.filter(x=>x.v>0).map(x=>{const share=x.v/sum*100;return`<span class="wh-power-seg ${x.p.toLowerCase()}" style="flex:0 0 ${share.toFixed(2)}%" title="${x.p} · ${ordinal(t.ranks[x.p])} in league · ${Math.round(x.v)} power"></span>`}).join('')}
function ordinal(n){const j=n%10,k=n%100;return n+(j===1&&k!==11?'st':j===2&&k!==12?'nd':j===3&&k!==13?'rd':'th')}
function cleanLegacyStarterLabels(card){const rec=card?.querySelector('.record');if(rec){rec.textContent=rec.textContent.replace(/\s·\sStarters\s+\S+\s·\sDepth\s+\S+/i,'')}const panel=card?.querySelector('.roster-panel');if(panel)panel.dataset.whPowerIgnoresCurrentStarters='1'}
function syncMySummary(sorted){const c=load(CTX,{}),id=String(c.my_roster_id||'');if(!id)return;const me=sorted.find(t=>String(t.r.roster_id)===id);if(!me)return;const sum=document.querySelector('#my-summary');if(!sum)return;const overall=sum.querySelector('.overall-rank');if(overall)overall.textContent=`#${me.ranks.overall}`;for(const chip of [...sum.querySelectorAll('.summary-ranks span')]){const txt=(chip.textContent||'').trim();if(/^Overall\b/i.test(txt))chip.textContent=`Overall ${ordinal(me.ranks.overall)}`;else if(/^Starters\b/i.test(txt)||/^Depth\b/i.test(txt))chip.remove()}}
function render(){if(!state.teams.size)return;styles();legend();const view=activeView(),parent=document.querySelector('#teams'),sorted=[...state.teams.values()].sort((a,b)=>view==='OVERALL'?a.ranks.overall-b.ranks.overall:a.ranks[view]-b.ranks[view]);for(const t of sorted){const id=String(t.r.roster_id),card=document.querySelector(`.team[data-team="${CSS.escape(id)}"]`);if(!card)continue;if(parent)parent.appendChild(card);card.classList.remove('top','mid','bottom');cleanLegacyStarterLabels(card);const rank=card.querySelector('.power-rank');if(rank)rank.textContent=`#${view==='OVERALL'?t.ranks.overall:t.ranks[view]}`;const chips=card.querySelectorAll('.pos-chip');POS.forEach((p,i)=>{if(chips[i])chips[i].innerHTML=`<b>${p}</b> ${ordinal(t.ranks[p])}`});const fill=card.querySelector('.power-fill'),meta=card.querySelectorAll('.bar-meta span');if(!fill)continue;const pct=view==='OVERALL'?overallPct(t):positionPct(t,view);fill.classList.add('wh-stacked-power');fill.style.setProperty('--wh-abs-width',`${pct.toFixed(1)}%`);fill.style.animation='none';fill.innerHTML=view==='OVERALL'?overallSegments(t):`<span class="wh-power-seg ${view.toLowerCase()}" style="flex:0 0 100%" title="${view} · ${ordinal(t.ranks[view])} in league · ${Math.round(t.m.pos[view]+t.bonus[view])} power"></span>`;void fill.offsetWidth;fill.style.animation='whAbsoluteGrow .72s cubic-bezier(.16,1,.3,1) forwards';if(meta[0])meta[0].textContent=view==='OVERALL'?'ROSTER STRENGTH':`${view} STRENGTH`;if(meta[1])meta[1].textContent='';card.dataset.whAbsolutePower=pct.toFixed(1);card.dataset.whPowerRank=String(t.ranks.overall)}syncMySummary([...state.teams.values()].sort((a,b)=>a.ranks.overall-b.ranks.overall))}
async function sync(){if(state.loading)return;const id=String(load(CTX,{}).league_id||'');if(!id)return;if(state.leagueId===id&&state.teams.size){render();return}state.loading=true;try{await loadPool();const [league,rosters]=await Promise.all([getJson(`${SLEEPER}/league/${id}`),getJson(`${SLEEPER}/league/${id}/rosters`)]);state.leagueId=id;state.league=league;state.rosters=Array.isArray(rosters)?rosters:[];state.teams.clear();build();render()}catch(e){console.warn('[Workhorse Power Bars]',e)}finally{state.loading=false}}
function bind(){document.addEventListener('click',e=>{if(e.target.closest?.('[data-power-view]'))setTimeout(render,45);if(e.target.closest?.('#connect-btn')||e.target.closest?.('#league-go')||e.target.closest?.('#wh-open-league')||e.target.closest?.('#refresh'))setTimeout(sync,180)},true);const head=document.querySelector('#league-head');if(head)new MutationObserver(()=>setTimeout(sync,140)).observe(head,{childList:true,subtree:true,characterData:true});const teams=document.querySelector('#teams');if(teams)new MutationObserver(()=>setTimeout(render,55)).observe(teams,{childList:true,subtree:true});setTimeout(sync,360);setTimeout(sync,1350)}
styles();bind();
})();